# LETS TRY SAMPLE 1:
# As a player I want to play a Fighter so that I can kick 
# butt and take names 

# """ 
# attacks roll is increased by 1 for every level instead 
# of every other level has 10 hit points 
# per level instead of 5
# """

class Fighter:
    def __init__(self, hp):
        self.hp = hp

    # function so that attacks roll is increased by 1 
    #  level instead of default every other level
    # def attack_roll(self):
    #     return self.level
        # attack advantage function
        # default (if not a fighter) - every other level-up - increases attack level by 1
        # a fighter - every level-up - increases attack level by 1
        #



    # function so that hp increases by 10 hp rather
    # than default 5 hp each level

    # def hit_points(self):
    #     return self.level 
        # hp (hit points) advantage function
        # default (if not a fighter) - every level-up - increases hp level by 5
        # a fighter - every level-up - increases hp level by 10
        



    # level is possibly the attack/hp? 

